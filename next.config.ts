import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // En Next.js, los archivos en la carpeta public se sirven automáticamente
  // desde la raíz del sitio. En Vercel, esto funciona sin configuración adicional.
  // Los archivos en public/ están disponibles en la raíz (ej: /favicon.png)
};

export default nextConfig;
